#include <stdio.h>

/**
 * main - entry point
 * Return: eachtime 0 (success)
 */

int main(void)
{
	char k;

	k = 'a';
	while
		(k >= 'a' && (k != 'q' || 'e'));
	{
		putchar(k);
		k++;
	}
	putchar('\n');
	return (0);
}
