#include <stdio.h>

/**
 * main - entry point
 * Return: eachtime 0 (success)
 */

int main(void)
{
	char w;

	w = 'z';
	while 
		(w >= 'a')
		{
			putchar(w);
			w--;
		}	
	putchar('\n');
	return (0);
}
